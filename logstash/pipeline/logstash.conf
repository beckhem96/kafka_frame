# ===== Input: Kafka에서 읽기 =====
input {
  # 일반 애플리케이션 로그 (서비스별)
  kafka {
    bootstrap_servers => "kafka:9092"
    topics_pattern => "app-logs-.*"  # app-logs-로 시작하는 모든 토픽
    group_id => "logstash-app-logs"
    consumer_threads => 4
    codec => json
    decorate_events => true  # Kafka 메타데이터 추가
  }

  # 에러 로그 (서비스별)
  kafka {
    bootstrap_servers => "kafka:9092"
    topics_pattern => "error-logs-.*"
    group_id => "logstash-error-logs"
    consumer_threads => 2
    codec => json
    decorate_events => true
  }

  # 접근 로그 (서비스별)
  kafka {
    bootstrap_servers => "kafka:9092"
    topics_pattern => "access-logs-.*"
    group_id => "logstash-access-logs"
    consumer_threads => 4
    codec => json
    decorate_events => true
  }

  # 감사 로그 (통합)
  kafka {
    bootstrap_servers => "kafka:9092"
    topics => ["audit-logs"]
    group_id => "logstash-audit-logs"
    consumer_threads => 2
    codec => json
    decorate_events => true
  }

  # 보안 로그 (통합)
  kafka {
    bootstrap_servers => "kafka:9092"
    topics => ["security-logs"]
    group_id => "logstash-security-logs"
    consumer_threads => 2
    codec => json
    decorate_events => true
  }

  # 비즈니스 이벤트 (서비스별)
  kafka {
    bootstrap_servers => "kafka:9092"
    topics_pattern => "business-events-.*"
    group_id => "logstash-business-events"
    consumer_threads => 4
    codec => json
    decorate_events => true
  }
}

# ===== Filter: 데이터 가공 =====
filter {
  # Kafka 토픽 이름 저장
  mutate {
    add_field => {
      "kafka_topic" => "%{[@metadata][kafka][topic]}"
    }
  }

  # 토픽 이름에서 서비스명 추출
  # 예: app-logs-order-service → service: order-service
  if [kafka_topic] =~ /.*-(.+)/ {
    grok {
      match => { "kafka_topic" => "(?<log_type>[^-]+)-(?<log_category>[^-]+)-(?<service_name>.+)" }
    }
  }

  # 타임스탬프 파싱
  date {
    match => [ "@timestamp", "ISO8601" ]
    target => "@timestamp"
  }
}

# ===== Output: Elasticsearch로 저장 =====
output {
  elasticsearch {
    hosts => ["http://elasticsearch:9200"]
    user => "elastic"
    password => "changeme"

    # 토픽 이름 그대로 인덱스로 사용 (날짜별 분리)
    # 예: app-logs-order-service-2026.01.20
    index => "%{kafka_topic}-%{+YYYY.MM.dd}"

    # 문서 ID (중복 방지)
    document_id => "%{[@metadata][kafka][key]}-%{[@metadata][kafka][offset]}"
  }

  # 디버깅용 (선택사항)
  # stdout { codec => rubydebug }
}